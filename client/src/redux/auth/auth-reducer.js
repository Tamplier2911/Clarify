const INITIAL_STATE = {
  currentUser: null,
  errorMessage: null
};

const authReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;
